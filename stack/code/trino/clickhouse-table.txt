CREATE TABLE default.user_clicks
(
    timestamp DateTime64(6),
    user_id UInt32,
    session_id String,
    product_id UInt32,
    action String,
    referrer String,
    user_agent String
) ENGINE = MergeTree()
ORDER BY timestamp;


